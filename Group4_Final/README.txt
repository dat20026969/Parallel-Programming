Google Colab không đọc được file .cpp hay .cu đi kèm ngoại trừ main.cu. Colab chỉ đọc được file .h.
 -> Tải từ `Code` để chạy trên Colab.
 -> Nên tải từ `Code-WithCpp` nếu chạy trên Visual Studio hoặc VSCode.